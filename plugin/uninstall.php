<?php
/**
 * The plugin preserves configuration by default.
 *
 * @package NotewareAdminTables
 */

declare(strict_types=1);

if (! defined('WP_UNINSTALL_PLUGIN')) {
    exit;
}

// Data removal will require an explicit administrator opt-in in a future release.
